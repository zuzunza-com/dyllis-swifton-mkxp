/**
 * Palcon-RGSS 코어 구현
 * 
 * RGSS 엔진의 핵심 로직을 구현합니다.
 * 이 파일은 wrapper.c에서 호출됩니다.
 */

#include <stdlib.h>
#include <string.h>
#include "binding/binding.h"

// 이 파일은 내부 구현을 위한 헬퍼 함수들을 포함합니다.
// 실제 엔진 로직은 각 모듈(graphics, audio, input 등)에서 구현됩니다.

// 내부 헬퍼 함수들 (필요시 사용)
// 현재는 wrapper.c에서 직접 구현되어 있음
